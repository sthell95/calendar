syntax = "proto3";

package calendar;

option go_package = "calendar.com/proto";

service Calendar {
  rpc Login (Credentials) returns (Token) {}
}

message Credentials {
  string login = 1;
  string password = 2;
}

message Token {
  string token = 1;
  int64 expires_at = 2;
}